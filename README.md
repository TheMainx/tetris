ai tetris bot using q learning

to use tetris.cpp library do this:

include "teris.h" and then u have all struct tetris to use in your program 

compile like this

the file structure is

tetris.cpp
tetris.h
bot.cpp

g++ bot.cpp tetris.cpp -o bot

and then just

./bot

to run bot
